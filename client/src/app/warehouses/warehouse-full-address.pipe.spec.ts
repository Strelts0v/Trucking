import { WarehouseFullAddressPipe } from './warehouse-full-address.pipe';

describe('WarehouseFullAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new WarehouseFullAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
